import React from "react";
import Toolbar from "./Toolbar";
import { mapsToolbarButtons } from "../../config/toolbarConfig";

const ToolbarMaps = () => {
  return <Toolbar buttons={mapsToolbarButtons} />;
};

export default ToolbarMaps;
